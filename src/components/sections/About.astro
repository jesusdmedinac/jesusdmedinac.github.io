---
import Section from "../Section.astro";

import { basics as defaultBasics } from "@cv";

interface Props {
	cv?: any;
}

const { cv } = Astro.props;
const { summary } = cv ? cv.basics : defaultBasics;
---

<Section title="About">
	<p>
		{summary}
	</p>
</Section>
